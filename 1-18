#include <stdio.h>
#include <ctype.h>
int main ()
{
  char c;
  int a=0;
  int i=0;
  char str[]="esta es la oracion\n";
  while (str[i])
  {
    a=a+1;
    c=str[i];
    if (isspace(c))
        c=str[a];
        str[i+1];
    putchar (c);
    i++;
  }
  return 0;

}
