#include <stdio.h>

#define MAX 100

int main(void)
{
	int i = 0;
	int	lim = MAX;
	int	c;
	char s[MAX];

	while (i < (lim - 1))
	{
		c = getchar();

		if (c == EOF)
			break;
		if (c == '\n')
			break;

		s[i++] = c;
	}

	s[i] = '\0';

	return 0;
}
