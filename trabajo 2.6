#include <iostream>
using namespace std;

int main(){
        int a=0,i,n,b;
        cout<<"Ingrese numero";
		cin>>n;
		if (n==1){
		   cout<<"si es primo";
		   return 0;
		}
		for(i=1;i<(n+1);i++){
            if(n%i==0){
                a++;
                b=n;
		   }
		}
		if(a!=2){
		     cout<<"almenos un numero no es Primo";
		     return 0;
		}else{
		     cout<<"Si es Primo";
		     b=b+b;
		}
    return 0;

}
